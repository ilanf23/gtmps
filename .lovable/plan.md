

# Remove the "Deeper" Section from the Homepage

Remove the `DeeperSection` component from the main homepage (`/`).

## Changes

### 1. `src/pages/Index.tsx`
- Remove the `import DeeperSection` line
- Remove `<DeeperSection />` from the JSX

One file, two line deletions. The `/1` copy retains its own `DeeperSection`.

