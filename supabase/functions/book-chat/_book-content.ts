// Book content placeholder. Will be regenerated from the uploaded GTM book PDF.
// To update: extract text from the PDF and replace BOOK_CONTENT below, then
// flip BOOK_AVAILABLE to true.

export const BOOK_AVAILABLE = false;

export const BOOK_CONTENT = `
[Book content will be loaded here once the GTM book PDF is uploaded.]

Until then, the assistant relies on the publicly known framework concepts:
- The Five Orbits (Core Proof, Active, Dead Zone, Warm Adjacency, New Gravity)
- The Five Layers (DISCOVER, PROVE, DESIGN, ACTIVATE, COMPOUND)
- The Dead Zone concept
- The formula: Signal + Proof + Context = Response
`.trim();
